/* 
Think of a variable as a container or "Cup".
They store info.
Put info into a container (cup) and we can then "pour" two half cups into another cup.  
We can put multiple containers into one larger container.

Var (variables) store value.  Containers for values.
*/

var x;
// the var then needs initalization.  This states the value of var.

var x = 1;
var z = "Eric Winebrenner";
console.log(x);
console.log(z);

const feetInMile = 5260;
console.log(feetInMile);

//naming is open and can be title anything we desire.  It should be best practice to be sensible and understood by fellow devs.

var age = 40;
var ageInAugust = age + 1;
console.log(ageInAugust);  //41

var eleven = 11;
console.log(eleven * eleven); //121

var nineteen = 19;
console.log(age - nineteen); //21

/* A var CANNOT start with a number */
//  console.log(2nite); // won't work

/* Reserved Words 
Here is a list of reserved words in JavaScript and should NOT be used in var naming

abstract
boolean
break
byte
case
catch
char
class
const
sontinue
debugger
default
delete
do
double
*/

